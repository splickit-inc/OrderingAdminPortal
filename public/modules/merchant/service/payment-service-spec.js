describe('paymentService', function() {

  beforeEach(module('adminPortal.merchant'));

  it('should ...', inject(function(paymentService) {

    //expect(paymentService.doSomething()).toEqual('something');

  }));

});
