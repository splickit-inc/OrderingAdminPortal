describe('statementService', function() {

  beforeEach(module('adminPortal.merchant'));

  it('should ...', inject(function(statementService) {

    //expect(statementService.doSomething()).toEqual('something');

  }));

});
