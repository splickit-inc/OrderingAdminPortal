describe('reportsGroupBySelections', function() {

  beforeEach(module('adminPortal.reports'));

  it('should ...', inject(function(reportsGroupBySelections) {

    //expect(reportsGroupBySelections.doSomething()).toEqual('something');

  }));

});
