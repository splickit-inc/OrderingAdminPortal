describe('scheduledReports', function() {

  beforeEach(module('adminPortal.reports'));

  it('should ...', inject(function(scheduledReports) {

    //expect(scheduledReports.doSomething()).toEqual('something');

  }));

});
